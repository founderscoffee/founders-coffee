{
  "name": "infra",
  "projectType": "library",
  "sourceRoot": "libs/infra/src",
  "tags": ["type:lib", "layer:shared", "domain:infra"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "libs/infra"
      }
    }
  }
}
