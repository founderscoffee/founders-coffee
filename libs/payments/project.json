{
  "name": "payments",
  "projectType": "library",
  "sourceRoot": "libs/payments/src",
  "tags": ["type:lib", "layer:server", "domain:payments"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "libs/payments"
      }
    }
  }
}
