{
  "name": "db",
  "projectType": "library",
  "sourceRoot": "libs/db/src",
  "tags": ["type:lib", "layer:data", "domain:db"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "libs/db"
      }
    },
    "generate-migrations": {
      "executor": "nx:run-commands",
      "options": {
        "command": "drizzle-kit generate",
        "cwd": "libs/db"
      }
    }
  }
}
