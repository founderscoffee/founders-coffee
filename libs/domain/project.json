{
  "name": "domain",
  "projectType": "library",
  "sourceRoot": "libs/domain/src",
  "tags": ["type:lib", "layer:domain", "domain:domain"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --coverage",
        "cwd": "libs/domain"
      }
    }
  }
}
