{
  "name": "domain-payments",
  "projectType": "library",
  "sourceRoot": "libs/domain/payments/src",
  "tags": ["type:lib", "layer:domain", "domain:payments"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "libs/domain/payments"
      }
    }
  }
}
