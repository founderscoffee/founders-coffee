{
  "name": "server-fns",
  "projectType": "library",
  "sourceRoot": "libs/server-fns/src",
  "tags": ["type:lib", "layer:server", "domain:server"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": [
        {
          "projects": ["i18n"],
          "target": "generate-i18n"
        }
      ],
      "options": {
        "command": "vitest run --coverage",
        "cwd": "libs/server-fns"
      }
    }
  }
}
