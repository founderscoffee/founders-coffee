{
  "name": "auth",
  "projectType": "library",
  "sourceRoot": "libs/auth/src",
  "tags": ["type:lib", "layer:server", "domain:auth"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "libs/auth"
      }
    }
  }
}
